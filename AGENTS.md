# 利刃的第二大脑 - 宪法级机器红线

## 1. 物理四元立柱
| 立柱 | 角色 | 机器硬约束 |
| :--- | :--- | :--- |
| **`index.md`** | 空间驾驶舱 | 🚫 严禁工具读写与修改。 |
| **`log.md`** | 时间序列流水 | ⚡ 严禁直接编辑或全量读取；必须且仅由 `gatekeeper.py append-log` 追加锁尾。 |
| **`glossary.md`** | 受控词表 | 🔍 严禁直读直改；唯一入口: `gatekeeper.py query-terms` / `flush-terms`。 |
| **`raw_manifest.json`** | 状态真值账本 | ⚡ 严禁直接编辑；必须且仅由 `gatekeeper.py commit` 维护。 |

---

## 2. 工序调度指针
```python
if user_intent in ["摄取新文献", "推进门禁队列", "维护wiki卡片", "扫描幽灵主题", "合成洞察", "合流词表"]:
    assert activate_skill("wiki-curator")
```

---

## 3. 机器红线断言
1. **物理范围边界**：仅限 `raw/` 根目录物理文件，子文件夹（如 `raw/fixtures/`）彻底忽略。
2. **工具路由边界**：单篇阅读唯一合法工具为 `view_file`，严禁调用脚本读文件。
3. **权威标准门禁**：`L6_informal` 一票否决，禁止作为事实标准；机理类主题标准字段强制填 `null`。
4. **历史范式保护**：严禁用早年文献推翻晚年成熟标准；数学公理与算法奠基永久保真。
5. **人类手记神圣禁区**：卡片中 `## 4. 个人思考与实战手记` **绝对禁读、禁写、禁改、禁删**。
6. **严禁命令探测**：🚫 **绝对禁止调用任何 `--help` 命令探测！**
7. **写盘参数约束**：向工作区（`wiki/`、`.scratch/` 等）写盘时，🚫 **绝对严禁携带 `ArtifactMetadata` 参数**。
8. **测试流水线隔离**：`run_tests.py` 仅限代码迭代验收，日常摄取绝对严禁调用。
9. **脚本源码绝对禁窥**：🚫 **绝对严禁调用 `view_file` 或检索工具阅读底层脚本源码（包括 `gatekeeper.py`、`slice_raw.py`、`scan_ghosts.py` 等）；必须严格作为确定性黑盒 CLI 接口调用。**
10. **合成沙箱隔离**：检索、雷达与驾驶舱严格排除 `wiki/insights/` 中 `status: draft` 页面。
11. **正向演进授权**：`## 2. 理论流变与共识演进时序表` 与 `## 5. 衍生研讨与前沿反常` 属于合法织网责任区，按认识论时序综合。
12. **链路暂存保护**：`.scratch/` 下的 `terms.json` 与 `log_entry.md` 严禁批量清空或物理删除。
13. **标准查词唯一入口**：严禁手写临时 Python 检索脚本或全局 grep 扫盘；全库受控词表与别名消歧唯一合法标准入口为 `python .scripts/gatekeeper.py query-terms`。
